<Window x:Class="WPFAPP.Pages.ApplicationPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Выбор приложений для белого списка" 
        Height="750" Width="1200"
        MinHeight="550" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <DropShadowEffect x:Key="SoftShadow" BlurRadius="20" ShadowDepth="4" Opacity="0.12" Color="#000000"/>
        <DropShadowEffect x:Key="CardShadow" BlurRadius="12" ShadowDepth="2" Opacity="0.06" Color="#000000"/>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#673AB7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                Effect="{StaticResource CardShadow}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#7C4DFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5E35B1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#2196F3"/>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#4CAF50"/>
        </Style>

        <Style x:Key="ListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,10"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Margin="4,1"
                                Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter Content="{TemplateBinding Content}" 
                                                  VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#EDE7F6"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="#F0F2F5" Effect="{StaticResource SoftShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Шапка -->
            <Border Grid.Row="0" 
                    Background="White" 
                    CornerRadius="16,16,0,0"
                    Padding="24,20"
                    BorderBrush="#E8E8E8"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <materialDesign:PackIcon Kind="Apps" Width="28" Height="28" Foreground="#673AB7" Margin="0,0,12,0"/>
                            <TextBlock Text="Выбор приложений для белого списка" 
                                       FontSize="20" FontWeight="Bold" 
                                       Foreground="#1A1A2E"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock x:Name="StatsText"
                                   Text="Сканирование системы..." 
                                   FontSize="13" 
                                   Foreground="#999999"/>
                    </StackPanel>

                    <Button Grid.Column="1" 
                            x:Name="CancelBtn"
                            Content="Отмена"
                            Width="100" Height="38"
                            Style="{StaticResource ModernButton}"
                            Background="#9E9E9E"
                            Click="CancelBtn_Click"
                            VerticalAlignment="Top"/>
                </Grid>
            </Border>

            <!-- Содержимое -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Поиск -->
                <Border Grid.Row="0" 
                        Background="White" 
                        CornerRadius="12" 
                        Margin="0,0,0,15"
                        Padding="16,12"
                        Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Grid.Column="0" 
                                                 Kind="Magnify" 
                                                 Width="20" 
                                                 Height="20" 
                                                 Foreground="#999999"
                                                 Margin="0,0,12,0"
                                                 VerticalAlignment="Center"/>
                        <TextBox x:Name="SearchTextBox" 
                                 Grid.Column="1"
                                 BorderThickness="0"
                                 FontSize="14"
                                 VerticalContentAlignment="Center"
                                 Background="Transparent"
                                 materialDesign:HintAssist.Hint="Поиск приложений..."
                                 TextChanged="SearchTextBox_TextChanged"/>
                    </Grid>
                </Border>

                <!-- Кнопки управления -->
                <WrapPanel Grid.Row="1" Margin="0,0,0,15">
                    <Button Content="Выбрать все" 
                            Width="130" Height="38"
                            Margin="0,0,10,0"
                            Style="{StaticResource SecondaryButton}"
                            Click="SelectAllBtn_Click">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="SelectAll" Width="16" Height="16" Margin="0,0,8,0"/>
                                    <TextBlock Text="Выбрать все"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <Button Content="Снять выделение" 
                            Width="140" Height="38"
                            Margin="0,0,10,0"
                            Style="{StaticResource ModernButton}"
                            Background="#F44336"
                            Click="DeselectAllBtn_Click">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="SelectionOff" Width="16" Height="16" Margin="0,0,8,0"/>
                                    <TextBlock Text="Снять выделение"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <Button Content="Обновить" 
                            Width="120" Height="38"
                            Margin="0,0,10,0"
                            Style="{StaticResource ModernButton}"
                            Background="#FF9800"
                            Click="RefreshBtn_Click">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16" Margin="0,0,8,0"/>
                                    <TextBlock Text="Обновить"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                </WrapPanel>

                <!-- Таблица приложений -->
                <Border Grid.Row="2" 
                        Background="White" 
                        CornerRadius="12" 
                        Effect="{StaticResource CardShadow}"
                        ClipToBounds="True">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Заголовки колонок (кликабельные) -->
                        <Border Grid.Row="0" 
                                Background="#F8F9FA" 
                                CornerRadius="12,12,0,0"
                                BorderBrush="#E9ECEF"
                                BorderThickness="0,0,0,1"
                                >
                            <Grid Height="45" Margin="16,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="45"/>
                                    <ColumnDefinition Width="220"/>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="110"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" 
                                           Text="" 
                                           FontWeight="SemiBold"
                                           Foreground="#666666"
                                           FontSize="12"
                                           HorizontalAlignment="Center"/>

                                <TextBlock x:Name="Header_Application" Grid.Column="1" 
                                           Tag="Приложение"
                                           Text="Приложение" 
                                           FontWeight="SemiBold"
                                           Foreground="#666666"
                                           FontSize="12"
                                           Cursor="Hand"
                                           MouseLeftButtonDown="Header_Click"/>

                                <TextBlock x:Name="Header_File" Grid.Column="2" 
                                           Tag="Файл"
                                           Text="Файл" 
                                           FontWeight="SemiBold"
                                           Foreground="#666666"
                                           FontSize="12"
                                           Cursor="Hand"
                                           MouseLeftButtonDown="Header_Click"/>

                                <TextBlock x:Name="Header_Path" Grid.Column="3" 
                                           Tag="Путь"
                                           Text="Путь" 
                                           FontWeight="SemiBold"
                                           Foreground="#666666"
                                           FontSize="12"
                                           Cursor="Hand"
                                           MouseLeftButtonDown="Header_Click"/>

                                <TextBlock x:Name="Header_Size" Grid.Column="4" 
                                           Tag="Размер"
                                           Text="Размер" 
                                           FontWeight="SemiBold"
                                           Foreground="#666666"
                                           FontSize="12"
                                           HorizontalAlignment="Right"
                                           Cursor="Hand"
                                           MouseLeftButtonDown="Header_Click"/>

                                <TextBlock x:Name="Header_Status" Grid.Column="5" 
                                           Tag="Статус"
                                           Text="Статус" 
                                           FontWeight="SemiBold"
                                           Foreground="#666666"
                                           FontSize="12"
                                           HorizontalAlignment="Center"
                                           Cursor="Hand"
                                           MouseLeftButtonDown="Header_Click"/>
                            </Grid>
                        </Border>

                        <!-- ListView с правильной виртуализацией -->
                        <ListView x:Name="ApplicationsListView"
                                  Grid.Row="1"
                                  SelectionMode="Extended"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  ItemContainerStyle="{StaticResource ListViewItemStyle}"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Width="45">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Click="CheckBox_Click">
                                                    <CheckBox.Template>
                                                        <ControlTemplate TargetType="CheckBox">
                                                            <Border x:Name="border" 
                                                                    Width="18" Height="18" 
                                                                    CornerRadius="4" 
                                                                    Background="White" 
                                                                    BorderBrush="#CCCCCC" 
                                                                    BorderThickness="1.5">
                                                                <materialDesign:PackIcon x:Name="checkIcon" 
                                                                                         Kind="Check" 
                                                                                         Width="12" Height="12" 
                                                                                         Foreground="#4CAF50"
                                                                                         Visibility="Collapsed"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsChecked" Value="True">
                                                                    <Setter TargetName="border" Property="BorderBrush" Value="#4CAF50"/>
                                                                    <Setter TargetName="checkIcon" Property="Visibility" Value="Visible"/>
                                                                </Trigger>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="BorderBrush" Value="#673AB7"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </CheckBox.Template>
                                                </CheckBox>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                    <GridViewColumn Width="220">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <materialDesign:PackIcon Kind="Application" 
                                                                             Width="18" Height="18" 
                                                                             Foreground="#673AB7"
                                                                             Margin="0,0,8,0"/>
                                                    <TextBlock Text="{Binding Name}" 
                                                               FontWeight="SemiBold"
                                                               FontSize="13"
                                                               Foreground="#333333"
                                                               TextTrimming="CharacterEllipsis"
                                                               VerticalAlignment="Center"
                                                               ToolTip="{Binding Name}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                    <GridViewColumn Width="180">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <materialDesign:PackIcon Kind="File" 
                                                                             Width="14" Height="14" 
                                                                             Foreground="#999999"
                                                                             Margin="0,0,6,0"/>
                                                    <TextBlock Text="{Binding FileName}" 
                                                               FontSize="12"
                                                               Foreground="#666666"
                                                               TextTrimming="CharacterEllipsis"
                                                               VerticalAlignment="Center"
                                                               ToolTip="{Binding FileName}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                    <GridViewColumn>
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding FilePath}" 
                                                           FontSize="11"
                                                           FontFamily="Consolas"
                                                           Foreground="#888888"
                                                           TextTrimming="CharacterEllipsis"
                                                           VerticalAlignment="Center"
                                                           ToolTip="{Binding FilePath}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                    <GridViewColumn Width="100">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding SizeFormatted}" 
                                                           FontSize="12"
                                                           Foreground="#666666"
                                                           HorizontalAlignment="Right"
                                                           VerticalAlignment="Center"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                    <GridViewColumn Width="110">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border CornerRadius="12" 
                                                        Padding="8,4"
                                                        Background="{Binding StatusColor}"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Status}" 
                                                               FontSize="11"
                                                               FontWeight="SemiBold"
                                                               Foreground="White"
                                                               HorizontalAlignment="Center"/>
                                                </Border>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>

                        <!-- Пустой список -->
                        <Border Grid.Row="0" Grid.RowSpan="2"
                                x:Name="EmptyListPanel"
                                Visibility="Collapsed"
                                Background="White"
                                CornerRadius="12">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="Apps" 
                                                         Width="72" 
                                                         Height="72" 
                                                         Foreground="#E0E0E0"
                                                         Margin="0,0,0,20"/>
                                <TextBlock Text="Приложения не найдены" 
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           Foreground="#999999"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="Попробуйте изменить критерии поиска" 
                                           FontSize="13"
                                           Foreground="#BBBBBB"
                                           HorizontalAlignment="Center"
                                           Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <!-- Нижняя панель с прогрессом и кнопкой -->
                <Border Grid.Row="3" 
                        Background="White" 
                        CornerRadius="12" 
                        Margin="0,15,0,0"
                        Padding="20,15"
                        Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <ProgressBar x:Name="ProgressBar" 
                                         Width="250" Height="6" 
                                         Visibility="Collapsed"
                                         Maximum="100" 
                                         Minimum="0"
                                         Foreground="#4CAF50"
                                         Background="#E0E0E0"/>
                            <TextBlock x:Name="ProgressText" 
                                       Text="" 
                                       Foreground="#666666"
                                       FontSize="12"
                                       Margin="12,0,0,0"
                                       Visibility="Collapsed"
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <Button Grid.Column="1" 
                                x:Name="AddSelectedBtn" 
                                Width="240" Height="44"
                                FontSize="14"
                                Style="{StaticResource SuccessButton}"
                                Click="AddSelectedBtn_Click"
                                IsEnabled="False">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="PlusCircle" Width="20" Height="20" Margin="0,0,10,0"/>
                                        <TextBlock Text="Добавить в белый список"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
<Window x:Class="WPFAPP.Pages.ApplicationPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Выбор приложений для белого списка" 
        Height="700" Width="1100"
        MinHeight="500" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#673AB7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="15,10"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5E35B1"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#9E9E9E"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Выберите приложения для добавления в белый список" 
                   FontSize="16" FontWeight="Bold" 
                   Foreground="#333333" Margin="0,0,0,10"/>

        <!-- Статистика -->
        <Border Grid.Row="1" 
                Background="#EDE7F6" 
                CornerRadius="4" 
                Padding="10" 
                Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="InformationOutline" 
                                         Width="18" Height="18" 
                                         Margin="0,0,8,0"
                                         Foreground="#673AB7"/>
                <TextBlock x:Name="StatsText" 
                           Text="Сканирование системы..." 
                           Foreground="#673AB7" 
                           FontWeight="Medium"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Поиск и кнопки -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchTextBox" 
                     Grid.Column="0"
                     materialDesign:HintAssist.Hint="Поиск по названию, файлу или пути..."
                     TextChanged="SearchTextBox_TextChanged"
                     VerticalContentAlignment="Center"
                     Padding="10,8"
                     FontSize="13"/>

            <Button Grid.Column="1" 
                    Content="Выбрать все" 
                    Margin="10,0,5,0"
                    Width="120"
                    Style="{StaticResource PrimaryButton}"
                    Click="SelectAllBtn_Click"/>

            <Button Grid.Column="2" 
                    Content="Снять все" 
                    Margin="5,0,10,0"
                    Width="110"
                    Style="{StaticResource PrimaryButton}"
                    Background="#FF9800"
                    Click="DeselectAllBtn_Click"/>

            <Button Grid.Column="3" 
                    Content="Обновить" 
                    Margin="5,0,5,0"
                    Width="100"
                    Style="{StaticResource PrimaryButton}"
                    Background="#4CAF50"
                    Click="RefreshBtn_Click"/>

            <Button Grid.Column="4" 
                    Content="X" 
                    Width="40"
                    Height="40"
                    Style="{StaticResource PrimaryButton}"
                    Background="#F44336"
                    Click="CancelBtn_Click"
                    ToolTip="Закрыть"/>
        </Grid>

        <!-- Список приложений -->
        <Border Grid.Row="3" 
                Background="White" 
                CornerRadius="4" 
                BorderBrush="#E0E0E0" 
                BorderThickness="1">
            <ListView x:Name="ApplicationsListView" 
                      SelectionMode="Extended">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="40">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Click="CheckBox_Click"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Приложение" Width="250">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="5,3">
                                        <TextBlock Text="{Binding Name}" 
                                                   FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding FileName}" 
                                                   FontSize="11"
                                                   Foreground="#666666"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Путь" Width="400">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FilePath}" 
                                               FontSize="11"
                                               ToolTip="{Binding FilePath}"
                                               TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Размер" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SizeFormatted}" 
                                               FontSize="11"
                                               HorizontalAlignment="Right"
                                               Margin="0,0,10,0"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Изменен" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding LastModifiedFormatted}" 
                                               FontSize="11"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Статус" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Status}" 
                                               Foreground="{Binding StatusColor}"
                                               FontSize="11"
                                               TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Кнопки действий -->
        <Grid Grid.Row="4" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" 
                        Orientation="Horizontal" 
                        VerticalAlignment="Center">
                <TextBlock x:Name="ProgressText" 
                           Text="" 
                           Foreground="#666666"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"
                           Visibility="Collapsed"/>
                <ProgressBar x:Name="ProgressBar" 
                             Width="300" 
                             Height="20" 
                             Visibility="Collapsed"
                             Maximum="100"
                             Minimum="0"/>
            </StackPanel>

            <Button Grid.Column="1" 
                    x:Name="AddSelectedBtn" 
                    Content="Добавить выбранные" 
                    Width="200" Height="45"
                    FontSize="14"
                    Style="{StaticResource PrimaryButton}"
                    Click="AddSelectedBtn_Click"
                    IsEnabled="False"/>
        </Grid>
    </Grid>
</Window>